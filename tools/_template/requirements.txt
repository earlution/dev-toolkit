# Python dependencies for Tool Name
# Add your dependencies here, one per line

# Example dependencies (remove if not needed):
# requests>=2.28.0
# click>=8.0.0
# pyyaml>=6.0

# Note: Use specific versions for reproducibility
# Use >= for minimum versions, == for exact versions

